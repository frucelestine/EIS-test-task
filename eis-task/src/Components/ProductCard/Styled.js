import styled from 'styled-components';
import { CardMedia, Card, Typography, Button } from '@material-ui/core';

export const CardContainer = styled(Card)({
    height: 20
});

export const CardHeading = styled(Typography)({
    height: 20
});

export const CardImage = styled(CardMedia)({
    height: 20
});

export const CardPrice = styled(Typography)({
    height: 20
});

export const CardQuantity = styled(Typography)({
    height: 20
});

export const CardColor = styled(Typography)({
    height: 20
});

export const CardButton = styled(Button)({
    height: 20
});

